import React from 'react';

//component imports
import Logo from '../../Logo/Logo';

const navigationBar = () => (
    <header>
        <Logo />
        <nav>
            
        </nav>
    </header>
);

export default navigationBar;